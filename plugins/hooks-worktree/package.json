{
  "name": "hooks-worktree",
  "version": "0.2.0",
  "description": "Git worktree automation - creates a worktree, copies .env files (monorepo-safe), and installs dependencies via detected package manager",
  "author": "Sangho",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test 'scripts/__tests__/*.test.js'"
  },
  "files": [
    ".claude-plugin/",
    "hooks/",
    "scripts/"
  ]
}
