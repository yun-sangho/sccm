{
  "name": "sandbox-presets",
  "version": "0.1.0",
  "description": "Vetted sandbox.* presets + /sandbox-presets:apply slash command",
  "author": "Sangho",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "test": "node --test 'scripts/__tests__/*.test.mjs'"
  },
  "files": [
    ".claude-plugin/",
    "commands/",
    "presets/",
    "scripts/"
  ]
}
